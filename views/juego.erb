<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
    <h1>Nivel facil!</h1>

    <table id="tablero">
      <% session["pokematch"].pokemones_encontrado.each_with_index do |fila, i| %>
        <tr>
          <% fila.each_with_index do |columna, j| %>
            <td>
              <a id=<%= "#{i}-#{j}" %> href="#">
                <img src="images/poke_bola.jpg" alt="Memory Card" width="96" height="96" style="margin:0 0 0 0px">
              </a>
            </td>
          <% end %>
        </tr>
       <% end %>
    </table>
  </body>
</html>
